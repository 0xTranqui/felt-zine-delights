"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.WarnIfEnvProjectDoesNotExistService = void 0;
const fs = require("fs");
const signale = require("signale");
class WarnIfEnvProjectDoesNotExistService {
    constructor(params = {}) {
        this._this = params._this;
    }
    async run() {
        const projectFile = '.env.project';
        // 1. write .env.project
        if (!fs.existsSync(projectFile)) {
            signale.fatal('Missing .env.project. To create it, run the command: dotenv-cli new');
            this._this.exit(1);
        }
    }
}
exports.WarnIfEnvProjectDoesNotExistService = WarnIfEnvProjectDoesNotExistService;
