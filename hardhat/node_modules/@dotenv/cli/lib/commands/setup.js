"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const command_1 = require("@oclif/command");
const append_to_gitignore_service_1 = require("../services/append-to-gitignore-service");
const setup_service_1 = require("../services/setup-service");
const deprecation_service_1 = require("../services/deprecation-service");
class Setup extends command_1.Command {
    async run() {
        const { argv } = this.parse(Setup);
        new deprecation_service_1.DeprecationService().run('new');
        // 1. create gitignore
        await new append_to_gitignore_service_1.AppendToGitignoreService().run();
        // 2. set up
        await new setup_service_1.SetupService(argv[0], argv[1]).run();
    }
}
exports.default = Setup;
Setup.description = 'set up .env, .env.project, and .env.me';
Setup.args = [
    {
        name: 'dotenv_project',
        required: true,
        description: 'Uniquely identifies the project',
    },
    {
        name: 'dotenv_me',
        required: true,
        description: "Uniquely authorizes you to access this project's .env file",
    },
];
